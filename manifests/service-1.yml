apiVersion: v1
kind: Service
metadata:
    name: "akscluster-11fe"
    labels:
        app: "akscluster-11fe"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akscluster-11fe"